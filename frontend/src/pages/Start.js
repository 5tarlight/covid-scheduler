import React, { Fragment } from 'react'
import { TodoTemplate } from '../components'

const Start = () => {
  return (
    <Fragment>
      <TodoTemplate>
        시작
      </TodoTemplate>
    </Fragment>
  )
}

export default Start
